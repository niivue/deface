node_modules
dist
# Staged from node_modules by scripts/copy-brainchop.mjs on every dev/build run:
# build output, versioned by the @brainchop/mindgrab dependency, not source.
public/brainchop
*.local
.DS_Store
test/smoke-*.png
audit_*.md
# Loose NIfTI outputs in the repo root (privacy hygiene); public/ fixtures stay tracked.
/*.nii
/*.nii.gz